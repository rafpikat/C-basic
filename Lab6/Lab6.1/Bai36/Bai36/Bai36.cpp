#include<iostream>
#include<cstring>

using namespace std;

int main() {
    string a, b;

    cin >> a >> b;

    a == b ? cout << "two people have the same name" : cout << "two people don't have the same name";

    return 0;
}